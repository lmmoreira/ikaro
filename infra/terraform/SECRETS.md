# Secrets — catalog, consumers, rotation

`modules/secrets` (M17-S16) creates Secret Manager **containers only** — no values, no IAM.
Per-SA accessor bindings are `modules/iam`'s job (M17-S17); the consumer column below is
documentation, not a Terraform artifact — S17 hand-maintains its own local map (secret → SA)
mirroring this table. Values are **never** managed by Terraform (M17 §2) — every secret's first
version is populated by the S27 (staging) / S37 (prod) activation runbooks via
`gcloud secrets versions add`.

| Secret | Consumer(s) | Source of truth | Notes |
|---|---|---|---|
| `db-password` | backend, migrate job | `DB_PASSWORD` (`apps/backend/src/config/env.validation.ts`) | Populated out-of-band — never generated by Terraform (rejected 2026-07-07: `tf-planner` credentials are PR-mintable and read state) |
| `jwt-secret` | backend, BFF, web | `JWT_SECRET` (backend + BFF env.validation.ts, web `.env`) | Same value across all three — web verifies the JWT in protected layouts |
| `internal-api-key` | backend, BFF | `INTERNAL_API_KEY` (backend + BFF env.validation.ts) | `InternalApiGuard` (M115-S03) shared secret gating BFF→backend calls. Web is not a consumer — verified 2026-07-18 that web's revalidate route checks only `hotsite-revalidate-secret`, and web's only `INTERNAL_API_KEY` references are Playwright E2E dev-login helpers |
| `platform-admin-key` | backend | `PLATFORM_ADMIN_KEY` (backend env.validation.ts) | UC-024 tenant provisioning; timing-safe check, sent via `X-Platform-Admin-Key` (S52) |
| `hotsite-revalidate-secret` | backend, web | `HOTSITE_REVALIDATE_SECRET` (backend env.validation.ts, web `.env`) | On-demand ISR revalidation (`/api/revalidate`) |
| `google-oauth-client-id` | BFF | `GOOGLE_CLIENT_ID` (BFF env.validation.ts) | Per-env OAuth client (S10) |
| `google-oauth-client-secret` | BFF | `GOOGLE_CLIENT_SECRET` (BFF env.validation.ts) | Per-env OAuth client (S10) |
| `brevo-smtp-key` | backend | `BREVO_SMTP_KEY` (backend env.validation.ts) | Per-env transactional-email key (S10) |
| `cloudflare-api-token` | infra pipeline (S22), CI purge (S41), backend (S40 runtime) | n/a — not in either app's env schema yet | **Prod only.** Two intentional homes (2026-07-08): a GitHub environment secret drives CI/CD (S23/S24); this Secret Manager container is for the runtime consumer once S40 lands (backend's Cloudflare verification adapter) |

## Rotation procedure

Secret Manager keeps every version; Terraform never touches values, so rotation is a manual,
audited operation:

1. Generate the new value out-of-band (never paste it into a shell history file or commit).
2. Add a new version — read the value into a shell variable first so it never appears as a
   literal command-line argument (and therefore never lands in shell history):

   ```bash
   IFS= read -r -s new_value
   printf '%s' "$new_value" | gcloud secrets versions add <secret-id> \
     --project=<ikaro-staging|ikaro-prod> --data-file=-
   unset new_value
   ```

3. **Force a new revision** of every consuming Cloud Run service. Cloud Run resolves a
   `secret_key_ref` pinned to `latest` only when a container starts — a running revision does
   **not** pick up the new version on its own, and a no-op `gcloud run services update` with no
   changed fields does **not** create a new revision either. Trigger a real redeploy: re-run the
   deploy pipeline (preferred), or force a spec change directly —
   `gcloud run services update <service> --region=southamerica-east1 --update-labels=secret-rotated-at=$(date +%s)`
   — then confirm the new revision reports healthy via `/health/ready`.
4. Once the new revision is confirmed healthy and no traffic is being served by the old one,
   disable (not destroy) the previous version:

   ```bash
   gcloud secrets versions disable <version-number> --secret=<secret-id> --project=<project>
   ```

   Destroy it only after a rollback window has passed with no incidents.
5. Record the rotation (secret id, date, operator) per the operator-local audit process —
   Cloud Audit Logs already capture the `gcloud` calls themselves (M17 §2).

`db-password` additionally requires updating the Cloud SQL user's password
(`gcloud sql users set-password`) in the same window as step 2, before any service is
redeployed — otherwise the new secret version won't authenticate.
