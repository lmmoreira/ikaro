# Dependencies
node_modules/

# Build outputs
dist/
.next/
coverage/
*.tsbuildinfo

# Documentation — manually formatted, not subject to Prettier
docs/
plan/
.agents/
.copilot/
*.md

# Generated / vendor files
pnpm-lock.yaml
**/next-env.d.ts

# Regenerated by `pnpm --filter @ikaro/infra-scripts run pubsub-catalog` via
# plain JSON.stringify(x, null, 2) — Prettier's JSON printer collapses short
# arrays onto one line, which disagrees with that output and made the
# regenerate-and-diff CI check (M17-S19) fail on cosmetic formatting alone.
infra/terraform/pubsub-catalog.json

# Worktrees (git-ignored; exclude from prettier check)
.claude/worktrees/

# Vendored skills — upstream content (each has its own VENDORED_FROM.md),
# not authored in this repo. *.md above already covered every vendored
# skill until skill-creator added non-.md assets (HTML eval-review tooling)
# — reformatting those on pull makes every future re-vendor diff noisy
# against upstream for no benefit, same reasoning as *.md above.
.claude/skills/
