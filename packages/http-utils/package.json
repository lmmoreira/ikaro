{
  "name": "@ikaro/http-utils",
  "version": "0.0.1",
  "private": true,
  "description": "Shared, framework-agnostic HTTP request utilities for backend, bff, and web",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "jest",
    "test:cov": "jest --coverage"
  },
  "devDependencies": {
    "@ikaro/config": "workspace:*",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.9.4",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11"
  }
}
