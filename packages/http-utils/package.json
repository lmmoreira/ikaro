{
  "name": "@ikaro/http-utils",
  "version": "0.0.1",
  "private": true,
  "description": "Shared, framework-agnostic HTTP request utilities for backend, bff, and web",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\" --no-error-on-unmatched-pattern --cache --cache-location .eslintcache --cache-strategy content",
    "test": "jest",
    "test:cov": "jest --coverage"
  },
  "devDependencies": {
    "@ikaro/config": "workspace:*",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.9.4",
    "eslint-plugin-jest": "^29.16.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11"
  }
}
