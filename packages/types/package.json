{
  "name": "@beloauto/types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types and DTOs",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@beloauto/config": "workspace:*"
  }
}
